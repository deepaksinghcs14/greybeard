{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}/scripts/greybeard.sh\" check --cwd \"$CLAUDE_PROJECT_DIR\" >/dev/null 2>&1 || true" }
        ]
      }
    ]
  }
}
