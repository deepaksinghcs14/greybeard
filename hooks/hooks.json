{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "command -v greybeard >/dev/null 2>&1 && greybeard check --cwd \"$CLAUDE_PROJECT_DIR\" || true" }
        ]
      }
    ]
  }
}
