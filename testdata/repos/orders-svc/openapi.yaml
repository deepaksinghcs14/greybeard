openapi: 3.0.3
info:
  title: orders-svc
  version: 1.0.0
paths:
  /orders:
    post:
      summary: Create an order
      responses:
        "201":
          description: created
  /orders/{id}:
    get:
      summary: Fetch an order
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ok
