// Fixture: orders-svc server code would live here. Kept minimal — extraction
// reads manifests, specs, and migrations, not server internals. (Go tooling
// ignores testdata/, so this file never compiles as part of greybeard.)
package main

func main() {}
